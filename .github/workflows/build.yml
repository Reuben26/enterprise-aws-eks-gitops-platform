name: Build and Push

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build Container
        run: |
          echo "Building claims-api image"

      - name: Validate Helm
        run: |
          echo "Validating Helm Charts"