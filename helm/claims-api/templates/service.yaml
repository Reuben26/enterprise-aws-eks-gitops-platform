apiVersion: v1
kind: Service
metadata:
  name: {{ include "claims-api.fullname" . }}
  labels:
    {{- include "claims-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "claims-api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP